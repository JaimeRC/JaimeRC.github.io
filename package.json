{
  "name": "personal-website",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "del": "rm -rf .astro dist",
    "dev": "astro dev",
    "start": "astro dev",
    "build": "npm run del && astro check && astro build",
    "preview": "npm run del && npm run build && astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.3",
    "@astrojs/sitemap": "^3.1.6",
    "astro": "^4.15.7",
    "astro-icon": "^1.1.1",
    "bootstrap": "^5.3.3",
    "typescript": "^5.6.2"
  },
  "devDependencies": {
    "@iconify-json/mdi": "^1.2.0"
  }
}
